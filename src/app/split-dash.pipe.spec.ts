import { SplitDashPipe } from './split-dash.pipe';

describe('SplitDashPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitDashPipe();
    expect(pipe).toBeTruthy();
  });
});
